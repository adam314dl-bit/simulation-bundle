import { describe, it } from 'vitest';

describe('GridRenderer', () => {
  it.todo('calls onDraw with grid rendering function');

  it.todo('click handler fires with cell coordinates {col, row}');

  it.todo('hover handler fires with cell coordinates');
});

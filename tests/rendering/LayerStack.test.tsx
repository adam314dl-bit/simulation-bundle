import { describe, it, expect } from 'vitest';

describe('LayerStack', () => {
  it.todo('renders children');

  it.todo('SVG overlay element exists when selectionMode is set');

  it.todo('children render in order (z-ordering)');
});

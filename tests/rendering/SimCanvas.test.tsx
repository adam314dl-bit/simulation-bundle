import { describe, it } from 'vitest';

describe('SimCanvas', () => {
  it.todo('renders a canvas element');

  it.todo('onDraw callback is called with ctx and viewport arguments');

  it.todo('canvas has touch-action: none style for pointer event handling');
});

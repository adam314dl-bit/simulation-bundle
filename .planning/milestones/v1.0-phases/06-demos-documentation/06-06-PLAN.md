---
phase: 06-demos-documentation
plan: 06
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05

must_haves:
  truths:
    - "README quick start gets a buyer from npm install to ecosystem demo in under 5 minutes"
    - "Component reference covers all 18 components with props table and usage example"
    - "Create Your Own Simulation guide covers state types, tickFn, ParameterSchema, renderer selection"
    - "Theming guide documents all --sim-* CSS variables"
    - "Performance guide covers grid size limits, particle counts, and rAF tips"
  artifacts:
    - path: "README.md"
      provides: "Complete project documentation"
      contains: "Quick Start"
      min_lines: 300
  key_links:
    - from: "README.md"
      to: "src/types/index.ts"
      via: "documents TickFn, ParameterSchema, SimConfig types"
      pattern: "TickFn"
    - from: "README.md"
      to: "src/theme/index.css"
      via: "documents --sim-* CSS variables"
      pattern: "--sim-"
---

<objective>
Write comprehensive README documentation with Quick Start, Component Reference, Create Your Own Simulation guide, Theming guide, and Performance guide.

Purpose: Enable buyers to go from install to running simulation in under 5 minutes and understand all components.
Output: README.md with all 5 documentation sections.
</objective>

<execution_context>
@/Users/midoriya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/midoriya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-demos-documentation/06-RESEARCH.md
@.planning/phases/06-demos-documentation/06-02-SUMMARY.md
@.planning/phases/06-demos-documentation/06-03-SUMMARY.md
@.planning/phases/06-demos-documentation/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README Quick Start and Component Reference</name>
  <files>README.md</files>
  <read_first>
    src/types/index.ts,
    src/core/index.ts,
    src/rendering/index.ts,
    src/rendering/types.ts,
    src/controls/index.ts,
    src/controls/ParameterPanel.tsx,
    src/controls/PresetSelector.tsx,
    src/data/index.ts,
    src/data/types.ts,
    src/demos/index.ts,
    src/theme/index.css,
    package.json,
    .planning/phases/06-demos-documentation/06-RESEARCH.md
  </read_first>
  <action>
Create README.md with the following sections. All code examples as inline TypeScript snippets (locked decision: copy-paste friendly). Pure markdown (locked decision: no MDX/JSX).

**Header:**
```markdown
# sim-kit

18-component React library for building interactive simulation interfaces. From idea to interactive sim in minutes, not days.

## Features

- SimulationProvider with tick loop, playback controls, and history
- Canvas2D grid rendering with dirty-rect optimization (500x500 @ 30fps)
- WebGL2 particle rendering (100k particles @ 60fps) with trails
- D3-force graph visualization with interactive node manipulation
- Auto-generated parameter panels from schema
- Timeline scrubbing, preset selector, playback controls
- Live stats, sparkline charts, event logs, heatmaps, entity inspector
- Dark theme by default with CSS custom properties for full customization
- Three complete demo simulations included
```

**Quick Start (DOCS-01):**
Section heading: `## Quick Start`
Steps:
1. Install: `npm install sim-kit react react-dom zustand recharts d3-force`
2. Import CSS: `import 'sim-kit/style.css';`
3. 10-line minimal example:
```typescript
import { SimulationProvider, useSimulation } from 'sim-kit/core';
import { GridRenderer } from 'sim-kit/rendering';
import { PlaybackBar } from 'sim-kit/controls';

const tickFn = (entities: { grid: Uint8Array; width: number; height: number }) => {
  const next = new Uint8Array(entities.grid.length);
  // Your simulation logic here
  for (let i = 0; i < next.length; i++) next[i] = Math.random() > 0.5 ? 1 : 0;
  return { ...entities, grid: next };
};

const initial = { grid: new Uint8Array(100 * 100), width: 100, height: 100 };

export default function App() {
  return (
    <SimulationProvider tickFn={tickFn} initialEntities={initial}>
      <GridRenderer config={{ data: initial.grid, width: 100, height: 100, cellSize: 5 }} />
      <PlaybackBar />
    </SimulationProvider>
  );
}
```
4. Run: `npm run dev` -> open browser

5. "To see a full demo:" instructions pointing to the ecosystem demo import:
```typescript
import { EcosystemDemo } from 'sim-kit/demos';
// Render <EcosystemDemo /> for a complete predator-prey simulation
```

**Component Reference (DOCS-02):**
Section heading: `## Component Reference`

For each of the 18 components, provide:
- Component name as h3 heading
- Brief description (1 sentence)
- Props table (| Prop | Type | Default | Description |)
- Single usage example (3-10 lines)
- Import path

Components organized by layer:

**Core:** SimulationProvider, useSimulation

**Rendering:** SimCanvas, GridRenderer, LayerStack, ParticleRenderer, ForceGraph, colorRamps

**Controls:** ParameterPanel, TimelineControl, PlaybackBar, PresetSelector

**Data:** StatsPanel, MiniChart, EventLog, HeatmapOverlay, EntityInspector

Read the actual prop interfaces from the source files listed in read_first to ensure accuracy. Every prop in the interface must appear in the table. Use actual default values from the source code.

For useSimulation, document the returned selectors: tick, entities, running, speed, play, pause, step, stepBack, setSpeed, setParameter, resetParameters, seekToTick, logEvent.

For colorRamps, document the 6 built-in ramps and the API: `colorRamps.viridis(t)`, `getRampLUT(name)`, `createColorRamp(stops)`.
  </action>
  <verify>
    <automated>cd /Users/midoriya/Desktop/simulation-bundle && npx vitest run tests/docs/readme.test.ts --reporter=verbose 2>&1 | grep -E "DOCS-01|DOCS-02|PASS|FAIL" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at project root
    - Contains "## Quick Start" heading
    - Contains "npm install sim-kit" in Quick Start
    - Contains "## Component Reference" heading
    - Component Reference mentions all 18 components: SimulationProvider, useSimulation, SimCanvas, GridRenderer, LayerStack, ParticleRenderer, ForceGraph, colorRamps, ParameterPanel, TimelineControl, PlaybackBar, PresetSelector, StatsPanel, MiniChart, EventLog, HeatmapOverlay, EntityInspector, RingBuffer
    - Each component has a props table with | Prop | Type | Default | Description |
    - Each component has a usage code example with import statement
    - DOCS-01 and DOCS-02 tests pass
  </acceptance_criteria>
  <done>README has Quick Start (npm install to demo in under 5 minutes) and complete Component Reference with props tables for all 18 components.</done>
</task>

<task type="auto">
  <name>Task 2: Write Create Your Own Sim, Theming, and Performance guides</name>
  <files>README.md</files>
  <read_first>
    README.md,
    src/types/index.ts,
    src/theme/index.css,
    src/core/store.ts,
    .planning/phases/06-demos-documentation/06-RESEARCH.md,
    .planning/STATE.md
  </read_first>
  <action>
Append three additional sections to the existing README.md (after Component Reference).

**Creating Your Own Simulation (DOCS-03):**
Section heading: `## Creating Your Own Simulation`

Step-by-step guide:
1. **Define your state type** — show a TypeScript interface for entities
2. **Write your tick function** — show a TickFn with typed entities and params, emphasize returning NEW objects (never mutate)
3. **Define your parameter schema** — show a ParameterSchema with range, toggle, select examples
4. **Choose your renderer** — decision table:

| Data Shape | Renderer | When to Use |
|-----------|----------|-------------|
| 2D grid (Uint8Array) | GridRenderer | Cellular automata, game of life, heatmaps |
| Particle positions (Float32Array) | ParticleRenderer | Physics sims, flocking, particle systems |
| Graph (nodes + links) | ForceGraph | Networks, social sims, dependency graphs |
| Custom drawing | SimCanvas + onDraw | Anything else |

5. **Wire it up** — complete example composing SimulationProvider + renderer + ParameterPanel + PlaybackBar
6. **Add presets** — show Preset array + PresetSelector

Full working example (~30 lines) of a custom simulation from scratch.

**Theming (DOCS-04):**
Section heading: `## Theming`

Document ALL `--sim-*` CSS variables. Read src/theme/index.css to get the complete list. For each variable:
- Variable name
- Default value
- What it controls

Show override example:
```css
:root {
  --sim-bg: #ffffff;
  --sim-text: #111827;
  --sim-accent: #2563eb;
}
```

Mention: "All components use these variables. Override them once to re-theme the entire kit."

Mention Tailwind compatibility: "Layout uses Tailwind utility classes. Colors use CSS custom properties. You can use both."

**Performance (DOCS-05):**
Section heading: `## Performance`

Practical guidelines derived from project decisions (STATE.md):
- **Grid rendering:** Up to 500x500 cells at 30fps with dirty-rect optimization. Above 250k cells, consider OffscreenCanvas (v2 feature). Use GridRenderer's built-in dirty-rect tracking.
- **Particle rendering:** Up to 100k particles at 60fps via WebGL2. Falls back to Canvas2D automatically when WebGL2 unavailable (capped at ~10k for Canvas2D). Use Float32Array with stride 4 (x, y, vx, vy).
- **Force graphs:** SVG mode for <500 nodes, Canvas2D mode for 500+ nodes. Set `canvasMode={true}` for large graphs.
- **Tick function:** Keep under 2ms. Avoid allocating large arrays every tick (pre-allocate). Return new top-level objects but reuse unchanged sub-objects.
- **React re-renders:** Use granular selectors: `useSimulation(s => s.tick)` not `useSimulation(s => s)`. Use `useShallow` for object selectors.
- **rAF loop:** Tick loop runs in Zustand vanilla API outside React. Components subscribe to specific slices. MiniChart uses rAF gate to avoid degrading tick loop.
  </action>
  <verify>
    <automated>cd /Users/midoriya/Desktop/simulation-bundle && npx vitest run tests/docs/readme.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "## Creating Your Own Simulation" heading
    - Creating Your Own Sim section mentions TickFn, ParameterSchema, SimulationProvider
    - README.md contains "## Theming" heading
    - Theming section contains "--sim-bg", "--sim-text", "--sim-accent", "--sim-border"
    - README.md contains "## Performance" heading
    - Performance section mentions grid size limits (500x500), particle counts (100k), and rAF
    - All DOCS-01 through DOCS-05 tests pass
    - README.md is at least 300 lines
  </acceptance_criteria>
  <done>README complete with all 5 documentation sections: Quick Start, Component Reference, Creating Your Own Simulation, Theming, and Performance.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/docs/readme.test.ts` passes all DOCS-01 through DOCS-05 tests
- README.md contains all 5 major sections
- Code examples use correct import paths matching package.json exports
</verification>

<success_criteria>
README enables buyers to go from npm install to running simulation in under 5 minutes, with complete component documentation, guides, and performance tips.
</success_criteria>

<output>
After completion, create `.planning/phases/06-demos-documentation/06-06-SUMMARY.md`
</output>

---
phase: 02-canvas-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/rendering/viewport.test.ts
  - tests/rendering/color-ramps.test.ts
  - tests/rendering/SimCanvas.test.tsx
  - tests/rendering/GridRenderer.test.tsx
  - tests/rendering/LayerStack.test.tsx
  - src/rendering/types.ts
  - src/rendering/viewport.ts
  - src/rendering/color-ramp-data.ts
  - src/rendering/color-ramps.ts
autonomous: true
requirements:
  - UTIL-01
  - REND-02

must_haves:
  truths:
    - "Color ramps produce correct RGB values for boundary inputs (0, 0.5, 1)"
    - "Viewport screenToWorld/worldToScreen are mathematical inverses"
    - "All 6 built-in ramps (viridis, inferno, plasma, coolwarm, terrain, category10) are available"
    - "getRampLUT returns raw Uint8Array for performance path"
    - "createColorRamp generates custom 256-entry ramps from stops"
  artifacts:
    - path: "src/rendering/types.ts"
      provides: "Rendering type definitions (Viewport interface, GridConfig, DrawCallback, LayerProps, SelectionMode)"
    - path: "src/rendering/viewport.ts"
      provides: "Viewport class with screenToWorld/worldToScreen transforms"
      exports: ["Viewport"]
    - path: "src/rendering/color-ramps.ts"
      provides: "Color ramp API: colorRamps object, getRampLUT, createColorRamp"
      exports: ["colorRamps", "getRampLUT", "createColorRamp"]
    - path: "src/rendering/color-ramp-data.ts"
      provides: "Static 256-entry Uint8Array RGBA LUT data for 6 built-in ramps"
    - path: "tests/rendering/viewport.test.ts"
      provides: "Viewport math tests"
    - path: "tests/rendering/color-ramps.test.ts"
      provides: "Color ramp correctness tests"
  key_links:
    - from: "src/rendering/color-ramps.ts"
      to: "src/rendering/color-ramp-data.ts"
      via: "import static LUT data"
      pattern: "import.*color-ramp-data"
    - from: "src/rendering/viewport.ts"
      to: "src/rendering/types.ts"
      via: "implements Viewport interface"
      pattern: "import.*types"
---

<objective>
Foundation plan: test scaffolds for all Phase 2 components, rendering type definitions, Viewport class with coordinate transforms, and 6 built-in color ramps as 256-entry LUTs.

Purpose: Establish the pure-logic foundation (types, math, data) that SimCanvas, GridRenderer, and LayerStack all depend on. Tests first (Nyquist), then implementations.
Output: Test stubs for all Phase 2 components, working Viewport class, 6 color ramps with pure function API.
</objective>

<execution_context>
@/Users/midoriya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/midoriya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-canvas-rendering/02-CONTEXT.md
@.planning/phases/02-canvas-rendering/02-RESEARCH.md

@src/types/index.ts
@src/rendering/index.ts

<interfaces>
<!-- From src/types/index.ts — types renderers consume -->
```typescript
export interface SimulationState<TEntities = unknown> {
  tick: number;
  entities: TEntities;
  running: boolean;
  speed: SpeedPreset;
  parameters: Record<string, ParameterValue>;
  history: { capacity: number; size: number; get(index: number): TEntities | undefined; push(item: TEntities): void; };
  events: SimEvent[];
}

export type ParameterValue = number | boolean | string | [number, number];
```

<!-- From src/core/store.ts — store shape renderers read -->
```typescript
export interface SimStore<TEntities = unknown> {
  tick: number;
  running: boolean;
  speed: SpeedPreset;
  entities: TEntities;
  parameters: Record<string, ParameterValue>;
  // ... actions
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test scaffolds + rendering types</name>
  <files>
    tests/rendering/viewport.test.ts,
    tests/rendering/color-ramps.test.ts,
    tests/rendering/SimCanvas.test.tsx,
    tests/rendering/GridRenderer.test.tsx,
    tests/rendering/LayerStack.test.tsx,
    src/rendering/types.ts
  </files>
  <read_first>
    tests/utils/history-buffer.test.ts,
    tests/core/tick-loop.test.ts,
    src/types/index.ts,
    src/rendering/index.ts
  </read_first>
  <action>
Create test scaffolds for all Phase 2 components (Wave 0 Nyquist). Follow the existing test patterns from tests/utils/ and tests/core/.

**tests/rendering/viewport.test.ts:**
- Test Viewport class imported from `../../src/rendering/viewport`
- Test `screenToWorld(sx, sy)` returns `[worldX, worldY]` — identity at scale=1, tx=0, ty=0 should return same coords
- Test `worldToScreen(wx, wy)` is the inverse of screenToWorld: `worldToScreen(...screenToWorld(100, 200))` equals `[100, 200]`
- Test `zoomAt(cx, cy, delta)` — point under cursor stays fixed after zoom: `screenToWorld(cx, cy)` before === `screenToWorld(cx, cy)` after
- Test zoom clamps to [0.1, 20] range
- Test `pan(dx, dy)` shifts translation

**tests/rendering/color-ramps.test.ts:**
- Test `colorRamps.viridis(0)` returns `rgb(68,1,84)` (viridis start)
- Test `colorRamps.viridis(1)` returns `rgb(253,231,37)` (viridis end)
- Test `colorRamps.viridis(0.5)` returns a valid `rgb(...)` string
- Test all 6 ramps exist: viridis, inferno, plasma, coolwarm, terrain, category10
- Test `getRampLUT('viridis')` returns Uint8Array of length 1024 (256 * 4)
- Test `createColorRamp('test', [{position: 0, color: [0,0,0]}, {position: 1, color: [255,255,255]}])` returns a function that maps 0 to `rgb(0,0,0)` and 1 to `rgb(255,255,255)`
- Test boundary: values < 0 clamp to 0, values > 1 clamp to 1
- Test category10 with modulo: `colorRamps.category10(0)` and `colorRamps.category10(0.1)` return same color (discrete bins)

**tests/rendering/SimCanvas.test.tsx:**
- Stub: test that SimCanvas renders a canvas element
- Stub: test that onDraw callback is called with ctx and viewport arguments
- Stub: test that canvas has `touch-action: none` style

**tests/rendering/GridRenderer.test.tsx:**
- Stub: test that GridRenderer calls onDraw with grid rendering function
- Stub: test click handler fires with cell coordinates `{col, row}`
- Stub: test hover handler fires with cell coordinates

**tests/rendering/LayerStack.test.tsx:**
- Stub: test that LayerStack renders children
- Stub: test that SVG overlay element exists when selectionMode is set
- Stub: test that children render in order (z-ordering)

**src/rendering/types.ts:**
Define all rendering-specific types:
```typescript
export interface ViewportState {
  scale: number;
  tx: number;
  ty: number;
  dpr: number;
}

export type DrawCallback = (ctx: CanvasRenderingContext2D, viewport: ViewportState) => void;

export interface GridConfig {
  width: number;          // grid columns
  height: number;         // grid rows
  data: ArrayLike<number>;  // flat array of cell values
  cellSize?: number;      // default 8
  borderWidth?: number;   // default 0
  colorRamp?: string;     // ramp name, default 'viridis'
}

export interface CellEvent {
  col: number;
  row: number;
  value: number;
  worldX: number;
  worldY: number;
}

export type SelectionMode = 'rect' | 'lasso' | 'none';

export interface RectSelection {
  type: 'rect';
  bounds: { x: number; y: number; width: number; height: number };
}

export interface LassoSelection {
  type: 'lasso';
  vertices: [number, number][];
}

export type Selection = RectSelection | LassoSelection;

export interface LayerStackProps {
  children: React.ReactNode;
  selectionMode?: SelectionMode;
  onSelect?: (selection: Selection) => void;
  className?: string;
}

export interface SimCanvasProps {
  width?: number;
  height?: number;
  onDraw: DrawCallback;
  className?: string;
}

export interface GridRendererProps {
  config: GridConfig;
  onCellClick?: (event: CellEvent) => void;
  onCellHover?: (event: CellEvent | null) => void;
  highlightCell?: { col: number; row: number } | null;
}
```
  </action>
  <verify>
    <automated>cd /Users/midoriya/Desktop/simulation-bundle && npx vitest run tests/rendering/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/rendering/viewport.test.ts exists and contains `describe('Viewport'`
    - tests/rendering/color-ramps.test.ts exists and contains `describe('colorRamps'` and `rgb(68,1,84)`
    - tests/rendering/SimCanvas.test.tsx exists and contains `describe('SimCanvas'`
    - tests/rendering/GridRenderer.test.tsx exists and contains `describe('GridRenderer'`
    - tests/rendering/LayerStack.test.tsx exists and contains `describe('LayerStack'`
    - src/rendering/types.ts exports ViewportState, DrawCallback, GridConfig, CellEvent, SelectionMode, Selection, SimCanvasProps, GridRendererProps, LayerStackProps
    - viewport.test.ts and color-ramps.test.ts tests FAIL (RED phase -- implementations not yet written)
    - SimCanvas/GridRenderer/LayerStack test stubs may skip or fail (component stubs)
  </acceptance_criteria>
  <done>All 5 test files exist with meaningful test cases. types.ts exports all rendering types. Viewport and color ramp tests fail (RED) because implementations don't exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Viewport class implementation</name>
  <files>src/rendering/viewport.ts</files>
  <read_first>
    tests/rendering/viewport.test.ts,
    src/rendering/types.ts
  </read_first>
  <behavior>
    - Identity transform: screenToWorld(100, 200) at scale=1, tx=0, ty=0 returns [100, 200]
    - Inverse property: worldToScreen(...screenToWorld(x, y)) === [x, y] for any x, y
    - zoomAt preserves point under cursor: world coords at cursor unchanged after zoom
    - Zoom clamps to [0.1, 20]
    - pan(dx, dy) adds to tx, ty
    - applyMomentum reduces velocity by damping factor (0.92) per call
  </behavior>
  <action>
Create `src/rendering/viewport.ts` implementing the Viewport class:

```typescript
import type { ViewportState } from './types';

export class Viewport implements ViewportState {
  scale = 1;
  tx = 0;
  ty = 0;
  dpr = typeof window !== 'undefined' ? (window.devicePixelRatio || 1) : 1;

  // Momentum state
  private vx = 0;
  private vy = 0;
  private readonly DAMPING = 0.92;
  private readonly MIN_VELOCITY = 0.01;

  screenToWorld(sx: number, sy: number): [number, number] {
    return [(sx - this.tx) / this.scale, (sy - this.ty) / this.scale];
  }

  worldToScreen(wx: number, wy: number): [number, number] {
    return [wx * this.scale + this.tx, wy * this.scale + this.ty];
  }

  zoomAt(cx: number, cy: number, delta: number): void {
    const factor = delta > 0 ? 1 / 1.08 : 1.08;
    const newScale = Math.max(0.1, Math.min(20, this.scale * factor));
    const ratio = newScale / this.scale;
    this.tx = cx - ratio * (cx - this.tx);
    this.ty = cy - ratio * (cy - this.ty);
    this.scale = newScale;
  }

  pan(dx: number, dy: number): void {
    this.tx += dx;
    this.ty += dy;
  }

  startMomentum(vx: number, vy: number): void {
    this.vx = vx;
    this.vy = vy;
  }

  applyMomentum(): boolean {
    if (Math.abs(this.vx) < this.MIN_VELOCITY && Math.abs(this.vy) < this.MIN_VELOCITY) {
      this.vx = 0;
      this.vy = 0;
      return false;
    }
    this.tx += this.vx;
    this.ty += this.vy;
    this.vx *= this.DAMPING;
    this.vy *= this.DAMPING;
    return true;
  }

  reset(): void {
    this.scale = 1;
    this.tx = 0;
    this.ty = 0;
    this.vx = 0;
    this.vy = 0;
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/midoriya/Desktop/simulation-bundle && npx vitest run tests/rendering/viewport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/rendering/viewport.ts contains `export class Viewport`
    - src/rendering/viewport.ts contains `screenToWorld(sx: number, sy: number): [number, number]`
    - src/rendering/viewport.ts contains `worldToScreen(wx: number, wy: number): [number, number]`
    - src/rendering/viewport.ts contains `zoomAt(cx: number, cy: number, delta: number)`
    - src/rendering/viewport.ts contains `applyMomentum(): boolean`
    - src/rendering/viewport.ts contains `Math.max(0.1, Math.min(20,`
    - npx vitest run tests/rendering/viewport.test.ts exits 0
  </acceptance_criteria>
  <done>Viewport class passes all tests: identity transform, inverse property, zoom-at-cursor stability, zoom clamping [0.1, 20], pan, momentum damping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Color ramps implementation (6 built-in + custom)</name>
  <files>src/rendering/color-ramp-data.ts, src/rendering/color-ramps.ts</files>
  <read_first>
    tests/rendering/color-ramps.test.ts,
    src/rendering/types.ts
  </read_first>
  <behavior>
    - colorRamps.viridis(0) returns 'rgb(68,1,84)'
    - colorRamps.viridis(1) returns 'rgb(253,231,37)'
    - getRampLUT('viridis') returns Uint8Array of length 1024
    - createColorRamp produces function mapping 0->startColor, 1->endColor
    - Values outside [0,1] clamp
    - category10 maps 10 discrete bins
  </behavior>
  <action>
**src/rendering/color-ramp-data.ts:**
Generate static 256-entry RGBA Uint8Array data for all 6 ramps. Extract from matplotlib canonical sources:

- **viridis**: Starts at rgb(68,1,84) purple, ends at rgb(253,231,37) yellow. Perceptually uniform.
- **inferno**: Starts at rgb(0,0,4) near-black, ends at rgb(252,255,164) pale yellow.
- **plasma**: Starts at rgb(13,8,135) deep blue, ends at rgb(240,249,33) bright yellow.
- **coolwarm**: Diverging from rgb(59,76,192) cool blue through rgb(221,221,221) neutral white to rgb(180,4,38) warm red.
- **terrain**: Starts at rgb(51,51,153) deep water blue through rgb(0,153,0) green land to rgb(255,255,255) snow peaks.
- **category10**: 10 discrete colors from D3 categorical palette: #1f77b4, #ff7f0e, #2ca02c, #d62728, #9467bd, #8c564b, #e377c2, #7f7f7f, #bcbd22, #17becf. Each occupies 25-26 entries in the 256-entry LUT (floor(256/10) per category, last takes remainder).

Each ramp exported as: `export const VIRIDIS_DATA = new Uint8Array([...])` with 1024 values (256 entries * 4 RGBA).

For viridis, inferno, plasma: sample 256 evenly-spaced points from the canonical matplotlib colormap data. Store as literal Uint8Array values.

For category10: repeat each of the 10 colors for ceil(256/10) entries.

**src/rendering/color-ramps.ts:**
```typescript
import { VIRIDIS_DATA, INFERNO_DATA, PLASMA_DATA, COOLWARM_DATA, TERRAIN_DATA, CATEGORY10_DATA } from './color-ramp-data';

type ColorRampFn = (t: number) => string;

const rampRegistry: Record<string, Uint8Array> = {
  viridis: VIRIDIS_DATA,
  inferno: INFERNO_DATA,
  plasma: PLASMA_DATA,
  coolwarm: COOLWARM_DATA,
  terrain: TERRAIN_DATA,
  category10: CATEGORY10_DATA,
};

function makeRampFn(lut: Uint8Array): ColorRampFn {
  return (t: number): string => {
    const i = Math.max(0, Math.min(255, Math.round(t * 255))) * 4;
    return `rgb(${lut[i]},${lut[i + 1]},${lut[i + 2]})`;
  };
}

export const colorRamps = {
  viridis: makeRampFn(VIRIDIS_DATA),
  inferno: makeRampFn(INFERNO_DATA),
  plasma: makeRampFn(PLASMA_DATA),
  coolwarm: makeRampFn(COOLWARM_DATA),
  terrain: makeRampFn(TERRAIN_DATA),
  category10: makeRampFn(CATEGORY10_DATA),
};

export function getRampLUT(name: string): Uint8Array {
  const lut = rampRegistry[name];
  if (!lut) throw new Error(`Unknown color ramp: ${name}`);
  return lut;
}

export function createColorRamp(
  name: string,
  stops: { position: number; color: [number, number, number] }[]
): ColorRampFn {
  // Sort stops by position
  const sorted = [...stops].sort((a, b) => a.position - b.position);
  const lut = new Uint8Array(1024); // 256 * 4

  for (let i = 0; i < 256; i++) {
    const t = i / 255;
    // Find surrounding stops
    let lower = sorted[0]!;
    let upper = sorted[sorted.length - 1]!;
    for (let s = 0; s < sorted.length - 1; s++) {
      if (t >= sorted[s]!.position && t <= sorted[s + 1]!.position) {
        lower = sorted[s]!;
        upper = sorted[s + 1]!;
        break;
      }
    }
    const range = upper.position - lower.position;
    const frac = range === 0 ? 0 : (t - lower.position) / range;
    const idx = i * 4;
    lut[idx] = Math.round(lower.color[0] + frac * (upper.color[0] - lower.color[0]));
    lut[idx + 1] = Math.round(lower.color[1] + frac * (upper.color[1] - lower.color[1]));
    lut[idx + 2] = Math.round(lower.color[2] + frac * (upper.color[2] - lower.color[2]));
    lut[idx + 3] = 255;
  }

  // Register for getRampLUT access
  rampRegistry[name] = lut;

  return makeRampFn(lut);
}
```

For the color-ramp-data.ts file, generate the actual 256-entry arrays. Use matplotlib's canonical viridis/inferno/plasma data (widely available as JSON arrays of RGB triplets). The executor should generate these programmatically or embed the well-known values. Key verification points:
- viridis[0] = [68, 1, 84], viridis[255] = [253, 231, 37]
- inferno[0] = [0, 0, 4], inferno[255] = [252, 255, 164]
- plasma[0] = [13, 8, 135], plasma[255] = [240, 249, 33]
- coolwarm[0] = [59, 76, 192], coolwarm[128] ~= [221, 221, 221], coolwarm[255] = [180, 4, 38]
- terrain[0] = [51, 51, 153], terrain[255] = [255, 255, 255]
- category10[0] = [31, 119, 180] (#1f77b4)
  </action>
  <verify>
    <automated>cd /Users/midoriya/Desktop/simulation-bundle && npx vitest run tests/rendering/color-ramps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/rendering/color-ramp-data.ts contains `export const VIRIDIS_DATA = new Uint8Array`
    - src/rendering/color-ramp-data.ts contains `export const CATEGORY10_DATA = new Uint8Array`
    - src/rendering/color-ramps.ts contains `export const colorRamps`
    - src/rendering/color-ramps.ts contains `export function getRampLUT`
    - src/rendering/color-ramps.ts contains `export function createColorRamp`
    - npx vitest run tests/rendering/color-ramps.test.ts exits 0
    - Ramp data files produce correct boundary values: viridis(0) = rgb(68,1,84), viridis(1) = rgb(253,231,37)
  </acceptance_criteria>
  <done>All 6 color ramps produce correct values at boundaries. getRampLUT returns 1024-byte Uint8Arrays. createColorRamp generates custom ramps. All color ramp tests pass.</done>
</task>

</tasks>

<verification>
npx vitest run tests/rendering/viewport.test.ts tests/rendering/color-ramps.test.ts -- both pass
npx tsc --noEmit -- no type errors
All 5 test scaffold files exist under tests/rendering/
</verification>

<success_criteria>
- Viewport class passes all coordinate transform tests (identity, inverse, zoom stability, clamping)
- All 6 color ramps produce correct boundary colors
- getRampLUT returns Uint8Array(1024) for each ramp
- createColorRamp produces working custom ramps
- Test scaffolds exist for SimCanvas, GridRenderer, LayerStack (stubs for Wave 2-3)
- All rendering types exported from src/rendering/types.ts
</success_criteria>

<output>
After completion, create `.planning/phases/02-canvas-rendering/02-01-SUMMARY.md`
</output>

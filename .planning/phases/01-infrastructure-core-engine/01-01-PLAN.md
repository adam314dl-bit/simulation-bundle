---
phase: 01-infrastructure-core-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/setup.ts
  - tests/build.test.ts
  - tests/core/SimulationProvider.test.tsx
  - tests/core/tick-loop.test.ts
  - tests/utils/history-buffer.test.ts
  - tests/theme/custom-properties.test.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-07
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
  - CORE-06
  - UTIL-02
  - THEME-01

must_haves:
  truths:
    - "npx vitest run exits non-zero (tests exist and fail — RED state confirmed)"
    - "vitest.config.ts exists and configures jsdom environment"
    - "All test files listed in VALIDATION.md Wave 0 Requirements exist on disk"
    - "Test stubs import from src paths that will exist after Wave 2 plans execute"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration with jsdom environment and React testing setup"
    - path: "tests/setup.ts"
      provides: "Test environment setup (jsdom, @testing-library/react)"
    - path: "tests/build.test.ts"
      provides: "Stubs for INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-07"
    - path: "tests/core/SimulationProvider.test.tsx"
      provides: "Stubs for CORE-01, CORE-04"
    - path: "tests/core/tick-loop.test.ts"
      provides: "Stubs for CORE-02, CORE-05, CORE-06"
    - path: "tests/utils/history-buffer.test.ts"
      provides: "Stubs for CORE-03, UTIL-02"
    - path: "tests/theme/custom-properties.test.ts"
      provides: "Stubs for THEME-01"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/setup.ts"
      via: "setupFiles configuration"
      pattern: "setupFiles.*tests/setup"
    - from: "tests/build.test.ts"
      to: "dist/ directory"
      via: "fs.existsSync checks after vite build"
      pattern: "existsSync.*dist"
---

<objective>
Create the Vitest test scaffold that establishes the Nyquist-compliant automated verification baseline for all Phase 1 requirements. This plan produces failing test stubs (RED state) for every requirement that has an automated verification path, so subsequent plans can turn them green during implementation.

Purpose: Automated feedback loop cannot exist without test files. Wave 0 test stubs ensure no plan executes without a verification mechanism already in place.
Output: vitest.config.ts, tests/setup.ts, and 5 test stub files covering all automated Phase 1 requirement checks.
</objective>

<execution_context>
@/Users/midoriya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/midoriya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-core-engine/01-VALIDATION.md
@.planning/phases/01-infrastructure-core-engine/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vitest.config.ts and tests/setup.ts</name>
  <files>vitest.config.ts, tests/setup.ts</files>
  <read_first>
    - .planning/phases/01-infrastructure-core-engine/01-VALIDATION.md (test framework specs: Vitest 4.1.0, jsdom environment, quick run command)
    - .planning/phases/01-infrastructure-core-engine/01-RESEARCH.md (standard stack section — Vitest, @testing-library/react versions)
  </read_first>
  <action>
Create `vitest.config.ts` at the project root with the following exact content:

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./tests/setup.ts'],
    globals: true,
    include: ['tests/**/*.{test,spec}.{ts,tsx}'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      include: ['src/**/*.{ts,tsx}'],
      exclude: ['src/**/*.stories.*', 'src/**/*.test.*'],
    },
  },
  resolve: {
    alias: {
      'sim-kit/core': '/src/core/index.ts',
      'sim-kit/rendering': '/src/rendering/index.ts',
      'sim-kit/controls': '/src/controls/index.ts',
      'sim-kit/data': '/src/data/index.ts',
      'sim-kit/demos': '/src/demos/index.ts',
      'sim-kit': '/src/index.ts',
    },
  },
});
```

Create `tests/setup.ts` with the following exact content:

```typescript
import '@testing-library/jest-dom';
import { afterEach } from 'vitest';
import { cleanup } from '@testing-library/react';

// Clean up after each test to prevent memory leaks
afterEach(() => {
  cleanup();
});

// jsdom does not implement CSS custom properties on :root by default.
// This helper lets tests set and read --sim-* vars on document.documentElement.
// Real CSS parsing is tested via build output inspection (tests/build.test.ts),
// not via jsdom computed style (which does not support custom properties).
Object.defineProperty(window, 'CSS', {
  value: { supports: () => false },
  writable: true,
});
```

Note: `@testing-library/jest-dom` requires installing `@testing-library/jest-dom` as a dev dependency alongside `@testing-library/react@^16.3.2`. Add it: `npm install -D @testing-library/jest-dom`.
  </action>
  <verify>
    <automated>node -e "const f = require('fs'); if (!f.existsSync('vitest.config.ts')) process.exit(1); if (!f.existsSync('tests/setup.ts')) process.exit(1); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists at project root
    - vitest.config.ts contains `environment: 'jsdom'`
    - vitest.config.ts contains `setupFiles: ['./tests/setup.ts']`
    - vitest.config.ts contains `provider: 'v8'` (coverage)
    - vitest.config.ts contains alias entries for all 5 subpath exports (sim-kit/core, sim-kit/rendering, sim-kit/controls, sim-kit/data, sim-kit/demos) plus `sim-kit`
    - tests/setup.ts exists
    - tests/setup.ts contains `import '@testing-library/jest-dom'`
    - tests/setup.ts contains `cleanup()` in afterEach
  </acceptance_criteria>
  <done>vitest.config.ts and tests/setup.ts exist with correct content as specified above.</done>
</task>

<task type="auto">
  <name>Task 2: Create build integration test stubs (tests/build.test.ts)</name>
  <files>tests/build.test.ts</files>
  <read_first>
    - .planning/phases/01-infrastructure-core-engine/01-VALIDATION.md (Per-Task Verification Map rows for INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-07)
    - .planning/phases/01-infrastructure-core-engine/01-RESEARCH.md (Pattern 1 and Pattern 2 for expected dist/ output structure)
  </read_first>
  <action>
Create `tests/build.test.ts`. This test file runs `vite build` as a subprocess and inspects the `dist/` output. All tests start as FAILING stubs because `dist/` does not yet exist. The file must be structured so tests pass once the build pipeline (01-03-PLAN.md) completes.

```typescript
import { describe, it, expect, beforeAll } from 'vitest';
import { execSync } from 'node:child_process';
import { existsSync, readFileSync } from 'node:fs';
import { resolve } from 'node:path';

const root = resolve(__dirname, '..');

// Run build once before all checks. Skip if dist/ already fresh (CI optimization).
beforeAll(() => {
  execSync('npx vite build', { cwd: root, stdio: 'inherit' });
}, 120_000); // 2-min timeout for first build

describe('INFRA-02: Multi-entry build output', () => {
  it('dist/index.js exists (root barrel)', () => {
    expect(existsSync(resolve(root, 'dist/index.js'))).toBe(true);
  });
  it('dist/core/index.js exists', () => {
    expect(existsSync(resolve(root, 'dist/core/index.js'))).toBe(true);
  });
  it('dist/rendering/index.js exists', () => {
    expect(existsSync(resolve(root, 'dist/rendering/index.js'))).toBe(true);
  });
  it('dist/controls/index.js exists', () => {
    expect(existsSync(resolve(root, 'dist/controls/index.js'))).toBe(true);
  });
  it('dist/data/index.js exists', () => {
    expect(existsSync(resolve(root, 'dist/data/index.js'))).toBe(true);
  });
  it('dist/demos/index.js exists', () => {
    expect(existsSync(resolve(root, 'dist/demos/index.js'))).toBe(true);
  });
});

describe('INFRA-03: Subpath exports resolve', () => {
  it('dist/core/index.js is non-empty', () => {
    const content = readFileSync(resolve(root, 'dist/core/index.js'), 'utf-8');
    expect(content.length).toBeGreaterThan(10);
  });
});

describe('INFRA-04: CSS output contains --sim-* custom properties', () => {
  it('dist/style.css exists', () => {
    expect(existsSync(resolve(root, 'dist/style.css'))).toBe(true);
  });
  it('dist/style.css contains --sim-accent', () => {
    const css = readFileSync(resolve(root, 'dist/style.css'), 'utf-8');
    expect(css).toContain('--sim-accent');
  });
  it('dist/style.css contains --sim-bg', () => {
    const css = readFileSync(resolve(root, 'dist/style.css'), 'utf-8');
    expect(css).toContain('--sim-bg');
  });
});

describe('INFRA-05: Peer dependencies externalized', () => {
  it('dist/index.js does not contain bundled React (createElement string absent)', () => {
    const content = readFileSync(resolve(root, 'dist/index.js'), 'utf-8');
    // If React is bundled, the string 'function createElement' appears in output
    expect(content).not.toContain('function createElement(');
  });
  it('dist/core/index.js does not contain bundled zustand internals', () => {
    const content = readFileSync(resolve(root, 'dist/core/index.js'), 'utf-8');
    // If zustand is bundled, 'createStore' implementation appears in output
    // Check file is under 100KB (bundling peer deps makes it much larger)
    expect(Buffer.byteLength(content, 'utf-8')).toBeLessThan(100_000);
  });
});

describe('INFRA-07: TypeScript declarations emitted', () => {
  it('dist/index.d.ts exists', () => {
    expect(existsSync(resolve(root, 'dist/index.d.ts'))).toBe(true);
  });
  it('dist/core/index.d.ts exists', () => {
    expect(existsSync(resolve(root, 'dist/core/index.d.ts'))).toBe(true);
  });
});
```
  </action>
  <verify>
    <automated>node -e "const f = require('fs'); if (!f.existsSync('tests/build.test.ts')) process.exit(1); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/build.test.ts exists
    - tests/build.test.ts contains `describe('INFRA-02`
    - tests/build.test.ts contains `describe('INFRA-03`
    - tests/build.test.ts contains `describe('INFRA-04`
    - tests/build.test.ts contains `describe('INFRA-05`
    - tests/build.test.ts contains `describe('INFRA-07`
    - tests/build.test.ts contains `execSync('npx vite build'` in beforeAll
    - tests/build.test.ts contains `--sim-accent` string check against dist/style.css
    - tests/build.test.ts contains size check `toBeLessThan(100_000)` for peer dep externalization
  </acceptance_criteria>
  <done>tests/build.test.ts exists with describe blocks for INFRA-02 through INFRA-07 (excluding INFRA-06 which is manual-only).</done>
</task>

<task type="auto">
  <name>Task 3: Create core and utility test stubs</name>
  <files>
    tests/core/SimulationProvider.test.tsx,
    tests/core/tick-loop.test.ts,
    tests/utils/history-buffer.test.ts,
    tests/theme/custom-properties.test.ts
  </files>
  <read_first>
    - .planning/phases/01-infrastructure-core-engine/01-VALIDATION.md (Per-Task Verification Map for CORE-01 through CORE-06, UTIL-02, THEME-01)
    - .planning/phases/01-infrastructure-core-engine/01-RESEARCH.md (Pattern 3: Zustand createStore + React Context, Pattern 4: rAF tick loop, Pattern 5: Ring buffer — exact API shapes)
  </read_first>
  <action>
Create four test stub files. Each test must be structured so it FAILS before implementation (import errors or assertion failures) and PASSES after implementation.

**tests/core/SimulationProvider.test.tsx** — CORE-01, CORE-04:

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen, act } from '@testing-library/react';
import { SimulationProvider, useSimulation } from 'sim-kit/core';

describe('CORE-01: SimulationProvider renders children', () => {
  it('renders children without crashing', () => {
    render(
      <SimulationProvider tickFn={(entities: unknown) => entities}>
        <div data-testid="child">hello</div>
      </SimulationProvider>
    );
    expect(screen.getByTestId('child')).toBeInTheDocument();
  });

  it('throws when useSimulation is called outside provider', () => {
    const Broken = () => {
      useSimulation((s) => s.tick);
      return null;
    };
    expect(() => render(<Broken />)).toThrow('useSimulation must be used inside SimulationProvider');
  });
});

describe('CORE-04: useSimulation hook API', () => {
  it('exposes play, pause, step, stepBack, setSpeed, setParameter, resetParameters, seekToTick, logEvent actions', () => {
    let capturedApi: Record<string, unknown> = {};
    const Capture = () => {
      capturedApi = useSimulation((s) => ({
        play: s.play,
        pause: s.pause,
        step: s.step,
        stepBack: s.stepBack,
        setSpeed: s.setSpeed,
        setParameter: s.setParameter,
        resetParameters: s.resetParameters,
        seekToTick: s.seekToTick,
        logEvent: s.logEvent,
        subscribe: s.subscribe,
      }));
      return null;
    };
    render(
      <SimulationProvider tickFn={(e: unknown) => e}>
        <Capture />
      </SimulationProvider>
    );
    expect(typeof capturedApi.play).toBe('function');
    expect(typeof capturedApi.pause).toBe('function');
    expect(typeof capturedApi.step).toBe('function');
    expect(typeof capturedApi.stepBack).toBe('function');
    expect(typeof capturedApi.setSpeed).toBe('function');
    expect(typeof capturedApi.setParameter).toBe('function');
    expect(typeof capturedApi.resetParameters).toBe('function');
    expect(typeof capturedApi.seekToTick).toBe('function');
    expect(typeof capturedApi.logEvent).toBe('function');
    expect(typeof capturedApi.subscribe).toBe('function');
  });
});
```

**tests/core/tick-loop.test.ts** — CORE-02, CORE-05, CORE-06:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { render, act } from '@testing-library/react';
import { SimulationProvider, useSimulation } from 'sim-kit/core';

// Mock rAF for deterministic testing
let rafCallbacks: Array<(t: number) => void> = [];
let rafTime = 0;

function advanceRaf(ms: number) {
  rafTime += ms;
  const cbs = [...rafCallbacks];
  rafCallbacks = [];
  cbs.forEach((cb) => cb(rafTime));
}

beforeEach(() => {
  rafCallbacks = [];
  rafTime = 0;
  vi.stubGlobal('requestAnimationFrame', (cb: (t: number) => void) => {
    rafCallbacks.push(cb);
    return rafCallbacks.length;
  });
  vi.stubGlobal('cancelAnimationFrame', vi.fn());
});

afterEach(() => {
  vi.unstubAllGlobals();
});

describe('CORE-02: Fixed-timestep tick loop', () => {
  it('tick count increments when running after rAF advances', async () => {
    let tick = 0;
    const Capture = () => {
      tick = useSimulation((s) => s.tick);
      return null;
    };
    const { unmount } = render(
      <SimulationProvider tickFn={(e: unknown) => e}>
        <Capture />
      </SimulationProvider>
    );

    // Start playback then advance time
    await act(async () => {
      // play() action triggers running = true
    });

    unmount();
    expect(typeof tick).toBe('number'); // Type check — full test after implementation
  });
});

describe('CORE-05: Speed multiplier presets', () => {
  it('accepts speed values 0.25, 0.5, 1, 2, 4, 8, 16', () => {
    const speeds = [0.25, 0.5, 1, 2, 4, 8, 16];
    // Stub test — will be expanded once SimulationProvider exists
    expect(speeds).toHaveLength(7);
    expect(speeds[0]).toBe(0.25);
    expect(speeds[6]).toBe(16);
  });
});

describe('CORE-06: Tick loop does not cause React re-renders', () => {
  it('is a stub that passes — real test verifies render count stays low during ticks', () => {
    // After implementation: render SimulationProvider with a render-counting child,
    // advance rAF 60 times, verify React renderCount < 5 (only initial + play change)
    expect(true).toBe(true);
  });
});
```

**tests/utils/history-buffer.test.ts** — CORE-03, UTIL-02:

```typescript
import { describe, it, expect } from 'vitest';
import { RingBuffer } from 'sim-kit/core';

describe('UTIL-02: RingBuffer — pre-allocated O(1) ring buffer', () => {
  it('push and get return correct values within capacity', () => {
    const buf = new RingBuffer<number>(5);
    buf.push(10);
    buf.push(20);
    buf.push(30);
    expect(buf.get(0)).toBe(10);
    expect(buf.get(1)).toBe(20);
    expect(buf.get(2)).toBe(30);
    expect(buf.size).toBe(3);
  });

  it('overwrites oldest items when full (capacity=5, push 6 items, get(0) returns item 2)', () => {
    const buf = new RingBuffer<number>(5);
    for (let i = 1; i <= 6; i++) buf.push(i);
    // After 6 pushes into cap=5: [2,3,4,5,6] — oldest is 2
    expect(buf.get(0)).toBe(2);
    expect(buf.get(4)).toBe(6);
    expect(buf.size).toBe(5);
    expect(buf.isFull).toBe(true);
  });

  it('latest returns most recently pushed item', () => {
    const buf = new RingBuffer<string>(3);
    buf.push('a');
    buf.push('b');
    expect(buf.latest).toBe('b');
  });

  it('get returns undefined for out-of-bounds index', () => {
    const buf = new RingBuffer<number>(5);
    buf.push(42);
    expect(buf.get(-1)).toBeUndefined();
    expect(buf.get(1)).toBeUndefined();
    expect(buf.get(100)).toBeUndefined();
  });

  it('clear resets size to 0 without reallocating', () => {
    const buf = new RingBuffer<number>(5);
    buf.push(1);
    buf.push(2);
    buf.clear();
    expect(buf.size).toBe(0);
    expect(buf.get(0)).toBeUndefined();
  });
});

describe('CORE-03: History ring buffer via SimulationProvider', () => {
  it('is a structural stub — verified via integration test after SimulationProvider implementation', () => {
    // Real test: SimulationProvider runs 1001 ticks, history.get(0) returns tick 2 state
    // Stub passes now, becomes meaningful in plan 01-05
    const buf = new RingBuffer<number>(1000);
    expect(buf.capacity).toBe(1000);
  });
});
```

**tests/theme/custom-properties.test.ts** — THEME-01:

```typescript
import { describe, it, expect } from 'vitest';
import { existsSync, readFileSync } from 'node:fs';
import { resolve } from 'node:path';

// THEME-01 verification: The CSS file declares all required --sim-* custom properties.
// jsdom does not evaluate @import "tailwindcss" blocks, so we inspect the source CSS file
// directly rather than querying computed styles. Production verification is via browser.
describe('THEME-01: --sim-* CSS custom properties declared', () => {
  const cssPath = resolve(__dirname, '../../src/theme/index.css');

  it('src/theme/index.css exists', () => {
    expect(existsSync(cssPath)).toBe(true);
  });

  const requiredTokens = [
    '--sim-bg',
    '--sim-surface',
    '--sim-surface-raised',
    '--sim-border',
    '--sim-text',
    '--sim-text-muted',
    '--sim-accent',
    '--sim-danger',
    '--sim-warning',
    '--sim-success',
    '--sim-font-family',
    '--sim-font-mono',
    '--sim-radius-sm',
    '--sim-radius-md',
    '--sim-spacing',
  ];

  requiredTokens.forEach((token) => {
    it(`declares ${token}`, () => {
      const css = readFileSync(cssPath, 'utf-8');
      expect(css).toContain(token);
    });
  });

  it('--sim-bg has value #0a0a0f (deep space dark)', () => {
    const css = readFileSync(cssPath, 'utf-8');
    expect(css).toContain('--sim-bg: #0a0a0f');
  });

  it('--sim-accent has value #6366f1 (indigo)', () => {
    const css = readFileSync(cssPath, 'utf-8');
    expect(css).toContain('--sim-accent: #6366f1');
  });
});
```
  </action>
  <verify>
    <automated>node -e "const f = require('fs'); ['tests/core/SimulationProvider.test.tsx','tests/core/tick-loop.test.ts','tests/utils/history-buffer.test.ts','tests/theme/custom-properties.test.ts'].forEach(p => { if (!f.existsSync(p)) { console.error('MISSING: ' + p); process.exit(1); } }); console.log('All test stubs present')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/core/SimulationProvider.test.tsx exists
    - tests/core/SimulationProvider.test.tsx contains `describe('CORE-01`
    - tests/core/SimulationProvider.test.tsx contains `describe('CORE-04`
    - tests/core/SimulationProvider.test.tsx contains `useSimulation must be used inside SimulationProvider`
    - tests/core/tick-loop.test.ts exists
    - tests/core/tick-loop.test.ts contains `describe('CORE-02`
    - tests/core/tick-loop.test.ts contains `describe('CORE-05`
    - tests/core/tick-loop.test.ts contains `describe('CORE-06`
    - tests/utils/history-buffer.test.ts exists
    - tests/utils/history-buffer.test.ts contains `RingBuffer` import from `sim-kit/core`
    - tests/utils/history-buffer.test.ts contains `get(0)).toBe(2)` (overflow wrap test)
    - tests/theme/custom-properties.test.ts exists
    - tests/theme/custom-properties.test.ts contains all 15 required tokens in `requiredTokens` array
    - tests/theme/custom-properties.test.ts contains `--sim-bg: #0a0a0f` exact value check
    - tests/theme/custom-properties.test.ts contains `--sim-accent: #6366f1` exact value check
  </acceptance_criteria>
  <done>Four test stub files exist with describe blocks mapped to their requirement IDs per VALIDATION.md.</done>
</task>

</tasks>

<verification>
After all tasks complete, verify the scaffold is in place:

```bash
# All test files present
ls tests/build.test.ts tests/core/SimulationProvider.test.tsx tests/core/tick-loop.test.ts tests/utils/history-buffer.test.ts tests/theme/custom-properties.test.ts tests/setup.ts vitest.config.ts

# Vitest recognizes the test files (will fail — that's expected RED state)
npx vitest run --reporter=verbose 2>&1 | head -40
```

Expected outcome: Tests are found and fail with import errors (no src/ files yet). This confirms RED state.
</verification>

<success_criteria>
- vitest.config.ts configures jsdom environment with all 6 path aliases and v8 coverage
- tests/setup.ts installs @testing-library/jest-dom and cleanup
- tests/build.test.ts has describe blocks for INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-07
- tests/core/SimulationProvider.test.tsx has describe blocks for CORE-01, CORE-04
- tests/core/tick-loop.test.ts has describe blocks for CORE-02, CORE-05, CORE-06
- tests/utils/history-buffer.test.ts has describe blocks for CORE-03, UTIL-02
- tests/theme/custom-properties.test.ts has describe blocks for THEME-01 with all 15 token assertions
- Running vitest exits non-zero (tests exist but fail — RED state confirmed)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-core-engine/01-01-SUMMARY.md`
</output>

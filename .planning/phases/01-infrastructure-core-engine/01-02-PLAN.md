---
phase: 01-infrastructure-core-engine
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.build.json
  - src/types/index.ts
  - src/theme/index.css
  - src/index.ts
  - src/core/index.ts
  - src/rendering/index.ts
  - src/controls/index.ts
  - src/data/index.ts
  - src/demos/index.ts
  - src/utils/index.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03
  - INFRA-05
  - INFRA-07
  - THEME-01
  - THEME-02

must_haves:
  truths:
    - "npx tsc -p tsconfig.build.json --noEmit exits 0 (TypeScript strict mode compiles clean)"
    - "src/types/index.ts exports all required public types"
    - "src/theme/index.css declares all 15 --sim-* custom properties with locked values"
    - "package.json name is 'sim-kit' with subpath exports for all 5 layers and sideEffects: ['*.css']"
    - "THEME-02: dist/style.css (produced by plan 01-03) contains Tailwind utility class selectors such as .flex, confirming @import 'tailwindcss' in src/theme/index.css is wired correctly through the build pipeline"
  artifacts:
    - path: "package.json"
      provides: "Package name, subpath exports, peerDependencies, sideEffects"
      contains: "\"name\": \"sim-kit\""
    - path: "tsconfig.json"
      provides: "Strict TypeScript config for IDE and Vitest"
      contains: "\"strict\": true"
    - path: "tsconfig.build.json"
      provides: "Build-only config, excludes tests/stories"
    - path: "src/types/index.ts"
      provides: "All exported public types"
      exports: ["SimulationState", "SimConfig", "TickFn", "PlaybackState", "SpeedPreset", "ParameterSchema", "SimEvent"]
    - path: "src/theme/index.css"
      provides: "CSS custom property declarations for all --sim-* tokens plus @import tailwindcss"
      contains: "--sim-bg: #0a0a0f"
  key_links:
    - from: "package.json exports"
      to: "dist/ files"
      via: "subpath export map"
      pattern: "\"./core\".*dist/core/index"
    - from: "src/types/index.ts"
      to: "src/core/store.ts (Wave 3)"
      via: "type imports"
      pattern: "export.*SimulationState"
    - from: "src/theme/index.css"
      to: "@tailwindcss/vite plugin"
      via: "@import tailwindcss directive"
      pattern: "@import \"tailwindcss\""
---

<objective>
Scaffold the complete project structure: package.json with subpath exports, TypeScript strict configs, all src/ layer directories with stub indexes, shared type definitions, and the CSS theme file. No implementation logic yet — this plan creates the contracts that subsequent plans implement against.

Purpose: Establishes import contracts (package.json exports, type definitions, layer entry points) that all downstream plans depend on.
Output: package.json, two tsconfig files, src/types/index.ts with all public types, src/theme/index.css with all 15 CSS tokens, and stub index.ts for all layer entry points.
</objective>

<execution_context>
@/Users/midoriya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/midoriya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-core-engine/01-CONTEXT.md
@.planning/phases/01-infrastructure-core-engine/01-RESEARCH.md
@.planning/phases/01-infrastructure-core-engine/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Research-derived contracts the executor implements against. From 01-RESEARCH.md. -->

Package structure (Pattern 2):
- name: "sim-kit", type: "module", sideEffects: ["*.css"]
- exports: "." / "./core" / "./rendering" / "./controls" / "./data" / "./demos" / "./style.css"
- peerDependencies: react ^19.0.0, react-dom ^19.0.0
- dependencies (bundled): zustand ^5.0.12, recharts ^3.8.0, d3-force ^3.0.0, d3-quadtree ^3.0.1, clsx ^2.1.0

TypeScript config (Pattern 8):
- moduleResolution: "bundler" (correct for Vite)
- strict: true + noUncheckedIndexedAccess + noImplicitOverride + exactOptionalPropertyTypes
- Two configs: tsconfig.json (IDE + tests), tsconfig.build.json (src only, emitDeclarationOnly)

CSS theme tokens (Pattern 6 + CONTEXT.md locked values):
- --sim-bg: #0a0a0f
- --sim-surface: #141420
- --sim-surface-raised: #1e1e2e
- --sim-border: #2a2a3a
- --sim-text: #e8e8ed
- --sim-text-muted: #8888a0
- --sim-accent: #6366f1
- --sim-danger: #ef4444
- --sim-warning: #f59e0b
- --sim-success: #22c55e
- --sim-font-family: system-ui, -apple-system, sans-serif
- --sim-font-mono: 'JetBrains Mono', 'Fira Code', ui-monospace, monospace
- --sim-radius-sm: 4px
- --sim-radius-md: 8px
- --sim-spacing: 4px
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json and install dependencies</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/01-infrastructure-core-engine/01-RESEARCH.md (Pattern 2: Package.json Subpath Exports, Standard Stack versions table)
    - .planning/phases/01-infrastructure-core-engine/01-CONTEXT.md (locked decisions: package name "sim-kit", subpath exports, tier split via zip)
  </read_first>
  <action>
Create package.json at project root with these exact contents:

{
  "name": "sim-kit",
  "version": "1.0.0",
  "description": "18-component React library for building interactive simulation interfaces",
  "type": "module",
  "files": ["dist"],
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": ["*.css"],
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" },
    "./core": { "types": "./dist/core/index.d.ts", "import": "./dist/core/index.js" },
    "./rendering": { "types": "./dist/rendering/index.d.ts", "import": "./dist/rendering/index.js" },
    "./controls": { "types": "./dist/controls/index.d.ts", "import": "./dist/controls/index.js" },
    "./data": { "types": "./dist/data/index.d.ts", "import": "./dist/data/index.js" },
    "./demos": { "types": "./dist/demos/index.d.ts", "import": "./dist/demos/index.js" },
    "./style.css": "./dist/style.css"
  },
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "dependencies": {
    "zustand": "^5.0.12",
    "recharts": "^3.8.0",
    "d3-force": "^3.0.0",
    "d3-quadtree": "^3.0.1",
    "clsx": "^2.1.0"
  },
  "devDependencies": {
    "vite": "~7.3.0",
    "@vitejs/plugin-react": "^6.0.1",
    "vite-plugin-dts": "^4.5.4",
    "vite-plugin-lib-inject-css": "^2.0.0",
    "typescript": "~5.9.3",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "tailwindcss": "^4.2.1",
    "@tailwindcss/vite": "^4.2.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "storybook": "^10.3.0",
    "@storybook/react-vite": "^10.3.0",
    "@storybook/addon-docs": "^10.3.0",
    "@storybook/addon-themes": "^10.3.0",
    "vitest": "^4.1.0",
    "@testing-library/react": "^16.3.2",
    "@testing-library/jest-dom": "^6.6.3",
    "@vitest/coverage-v8": "^4.1.0"
  }
}

After creating package.json, run: npm install
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf-8'));if(p.name!=='sim-kit')process.exit(1);if(!p.exports['./core'])process.exit(1);if(!Array.isArray(p.sideEffects))process.exit(1);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json exists with "name": "sim-kit"
    - package.json contains "type": "module"
    - package.json exports field has entries for ".", "./core", "./rendering", "./controls", "./data", "./demos", "./style.css"
    - package.json sideEffects is ["*.css"]
    - package.json peerDependencies has react ^19.0.0 and react-dom ^19.0.0
    - package.json dependencies includes zustand, recharts, d3-force, d3-quadtree, clsx
    - node_modules/ exists after npm install
  </acceptance_criteria>
  <done>package.json exists with correct sim-kit structure and npm install completes without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript configs</name>
  <files>tsconfig.json, tsconfig.build.json</files>
  <read_first>
    - .planning/phases/01-infrastructure-core-engine/01-RESEARCH.md (Pattern 8: TypeScript Strict Mode tsconfig — exact compiler options for both files)
  </read_first>
  <action>
Create tsconfig.json at project root:

{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "exactOptionalPropertyTypes": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "baseUrl": ".",
    "paths": {
      "sim-kit/*": ["src/*"],
      "sim-kit": ["src/index.ts"]
    }
  },
  "include": ["src", "stories", "tests", "vitest.config.ts", ".storybook"]
}

Create tsconfig.build.json at project root:

{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "declarationMap": true,
    "emitDeclarationOnly": true,
    "outDir": "dist",
    "noUnusedLocals": false,
    "noUnusedParameters": false
  },
  "include": ["src"],
  "exclude": ["src/**/*.test.*", "src/**/*.stories.*", "src/**/*.spec.*"]
}

Note: noUnusedLocals/noUnusedParameters are relaxed in tsconfig.build.json because stub layer index files (rendering, controls, data, demos) start empty. Re-enable in Phase 6 once all layers are populated.
  </action>
  <verify>
    <automated>node -e "const t=JSON.parse(require('fs').readFileSync('tsconfig.json','utf-8'));if(!t.compilerOptions.strict)process.exit(1);if(t.compilerOptions.moduleResolution!=='bundler')process.exit(1);console.log('OK')" && node -e "const b=JSON.parse(require('fs').readFileSync('tsconfig.build.json','utf-8'));if(!b.extends)process.exit(1);if(!b.compilerOptions.declaration)process.exit(1);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.json exists with "strict": true
    - tsconfig.json has "moduleResolution": "bundler"
    - tsconfig.json has "noUncheckedIndexedAccess": true
    - tsconfig.json has "exactOptionalPropertyTypes": true
    - tsconfig.json paths includes "sim-kit/*": ["src/*"]
    - tsconfig.json includes "src", "stories", "tests" in include array
    - tsconfig.build.json exists extending ./tsconfig.json
    - tsconfig.build.json has "declaration": true and "emitDeclarationOnly": true
    - tsconfig.build.json include is only ["src"]
    - tsconfig.build.json exclude contains "src/**/*.test.*"
  </acceptance_criteria>
  <done>tsconfig.json and tsconfig.build.json exist with strict TypeScript configuration as specified.</done>
</task>

<task type="auto">
  <name>Task 3: Create src/ directory structure, types, theme, and layer stubs</name>
  <files>
    src/types/index.ts,
    src/theme/index.css,
    src/core/index.ts,
    src/rendering/index.ts,
    src/controls/index.ts,
    src/data/index.ts,
    src/demos/index.ts,
    src/utils/index.ts,
    src/index.ts
  </files>
  <read_first>
    - .planning/phases/01-infrastructure-core-engine/01-RESEARCH.md (Recommended Project Structure, Pattern 6: CSS Custom Properties, Pattern 3: SimStore interface shape)
    - .planning/phases/01-infrastructure-core-engine/01-CONTEXT.md (locked decisions: token values, src/ directory layout)
    - .planning/REQUIREMENTS.md (INFRA-07: exported types; CORE-01 through CORE-06: SimulationState shape needed by store)
  </read_first>
  <action>
Create src/types/index.ts — all public TypeScript types exported from this single file:

```typescript
// src/types/index.ts
// All public types for sim-kit. Imported by all layer implementations.

/** Generic simulation state — buyers extend this with their domain state */
export interface SimulationState<TEntities = unknown> {
  tick: number;
  entities: TEntities;
  running: boolean;
  speed: SpeedPreset;
  parameters: Record<string, ParameterValue>;
  history: {
    capacity: number;
    size: number;
    get(index: number): TEntities | undefined;
    push(item: TEntities): void;
  };
  events: SimEvent[];
}

/** Simulation configuration passed to SimulationProvider */
export interface SimConfig<TEntities = unknown> {
  initialEntities: TEntities;
  tickFn: TickFn<TEntities>;
  parameters?: ParameterSchema;
  maxHistoryLength?: number;  // default: 1000
  keepRunning?: boolean;      // override background-tab auto-pause, default: false
}

/** User-supplied tick function: receives current entities + config, returns next entities */
export type TickFn<TEntities = unknown> = (
  entities: TEntities,
  config: Record<string, ParameterValue>
) => TEntities;

/** Playback state exposed via useSimulation */
export interface PlaybackState {
  tick: number;
  running: boolean;
  speed: SpeedPreset;
  historySize: number;
}

/** Speed multiplier presets (CORE-05) */
export type SpeedPreset = 0.25 | 0.5 | 1 | 2 | 4 | 8 | 16;

/** All valid speed values as a runtime array */
export const SPEED_PRESETS: SpeedPreset[] = [0.25, 0.5, 1, 2, 4, 8, 16];

/** A single parameter definition for ParameterPanel auto-generation */
export type ParameterSchema = Record<string, ParameterDef>;

export type ParameterDef =
  | { type: 'range'; min: number; max: number; step?: number; default: number; label?: string }
  | { type: 'toggle'; default: boolean; label?: string }
  | { type: 'select'; options: string[]; default: string; label?: string }
  | { type: 'color'; default: string; label?: string }
  | { type: 'vec2'; min: number; max: number; default: [number, number]; label?: string }
  | { type: 'group'; label: string; children: ParameterSchema };

/** Runtime parameter value types */
export type ParameterValue = number | boolean | string | [number, number];

/** A logged simulation event */
export interface SimEvent {
  id: string;
  tick: number;
  timestamp: number;
  type: string;
  severity: 'info' | 'warning' | 'critical';
  message: string;
  data?: Record<string, unknown>;
}
```

Create src/theme/index.css — all --sim-* tokens with exact locked values from CONTEXT.md:

```css
/* src/theme/index.css */
/* sim-kit design system — deep space dark aesthetic */
/* Buyers override --sim-* variables to rebrand without touching component code */

@import "tailwindcss";

:root {
  /* Backgrounds */
  --sim-bg: #0a0a0f;
  --sim-surface: #141420;
  --sim-surface-raised: #1e1e2e;

  /* Borders */
  --sim-border: #2a2a3a;

  /* Text */
  --sim-text: #e8e8ed;
  --sim-text-muted: #8888a0;

  /* Accent — override this one variable to rebrand */
  --sim-accent: #6366f1;

  /* State colors */
  --sim-danger: #ef4444;
  --sim-warning: #f59e0b;
  --sim-success: #22c55e;

  /* Typography */
  --sim-font-family: system-ui, -apple-system, sans-serif;
  --sim-font-mono: 'JetBrains Mono', 'Fira Code', ui-monospace, monospace;

  /* Shape */
  --sim-radius-sm: 4px;
  --sim-radius-md: 8px;

  /* Spacing unit — multiply for consistent scale */
  --sim-spacing: 4px;
}
```

Create stub layer index files. Each exports a placeholder comment so TypeScript does not error on an empty module. Real exports added in later plans.

src/core/index.ts:
```typescript
// Core layer — SimulationProvider, useSimulation, tick loop
// Implementations added in plan 01-05
// Placeholder export to satisfy module resolution
export type { SimulationState, SimConfig, TickFn, PlaybackState, SpeedPreset, ParameterSchema, ParameterDef, ParameterValue, SimEvent } from '../types/index';
export { SPEED_PRESETS } from '../types/index';
// RingBuffer exported here after plan 01-04 completes
// export { RingBuffer } from '../utils/history-buffer';
```

src/rendering/index.ts:
```typescript
// Rendering layer — SimCanvas, GridRenderer, LayerStack (Phase 2)
export {};
```

src/controls/index.ts:
```typescript
// Controls layer — ParameterPanel, TimelineControl, PlaybackBar, PresetSelector (Phase 3)
export {};
```

src/data/index.ts:
```typescript
// Data layer — StatsPanel, MiniChart, EventLog, HeatmapOverlay, EntityInspector (Phase 5)
export {};
```

src/demos/index.ts:
```typescript
// Demos layer — Ecosystem, Particles, SocialNetwork demos (Phase 6)
export {};
```

src/utils/index.ts:
```typescript
// Utilities — RingBuffer (UTIL-02), color ramps (UTIL-01, Phase 2), WebGL helpers (UTIL-03, Phase 4)
// RingBuffer added in plan 01-04
export {};
```

src/index.ts — root barrel re-exporting all layers:
```typescript
// sim-kit root barrel — re-exports all layers for convenient prototyping
// For production tree-shaking, import from subpaths: 'sim-kit/core', 'sim-kit/rendering', etc.
export * from './core/index';
export * from './rendering/index';
export * from './controls/index';
export * from './data/index';
// demos intentionally NOT re-exported from root — demos/ is the $49 tier
```

After creating all files, run: npx tsc -p tsconfig.build.json --noEmit
This should exit 0 with the stub types in place (no implementation errors yet).
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.build.json --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/types/index.ts exists
    - src/types/index.ts contains `export interface SimulationState`
    - src/types/index.ts contains `export type TickFn`
    - src/types/index.ts contains `export type SpeedPreset = 0.25 | 0.5 | 1 | 2 | 4 | 8 | 16`
    - src/types/index.ts contains `export const SPEED_PRESETS`
    - src/types/index.ts contains `export interface SimEvent`
    - src/types/index.ts contains `export type ParameterValue`
    - src/theme/index.css exists
    - src/theme/index.css contains `@import "tailwindcss"`
    - src/theme/index.css contains `--sim-bg: #0a0a0f`
    - src/theme/index.css contains `--sim-surface: #141420`
    - src/theme/index.css contains `--sim-border: #2a2a3a`
    - src/theme/index.css contains `--sim-text: #e8e8ed`
    - src/theme/index.css contains `--sim-text-muted: #8888a0`
    - src/theme/index.css contains `--sim-accent: #6366f1`
    - src/theme/index.css contains `--sim-danger: #ef4444`
    - src/theme/index.css contains `--sim-warning: #f59e0b`
    - src/theme/index.css contains `--sim-success: #22c55e`
    - src/theme/index.css contains `--sim-font-mono`
    - src/theme/index.css contains `--sim-radius-sm: 4px`
    - src/theme/index.css contains `--sim-radius-md: 8px`
    - src/theme/index.css contains `--sim-spacing: 4px`
    - src/core/index.ts exists and re-exports from ../types/index
    - src/rendering/index.ts, src/controls/index.ts, src/data/index.ts, src/demos/index.ts, src/utils/index.ts all exist
    - src/index.ts exists with export * from './core/index'
    - npx tsc -p tsconfig.build.json --noEmit exits 0
  </acceptance_criteria>
  <done>All src/ layer stubs created, types defined, theme CSS complete, and tsc --noEmit exits clean.</done>
</task>

</tasks>

<verification>
```bash
# TypeScript compiles clean
npx tsc -p tsconfig.build.json --noEmit

# All 15 CSS tokens present in theme file
grep -c "\-\-sim-" src/theme/index.css

# Package exports structure correct
node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf-8'));console.log(Object.keys(p.exports))"
# Expected: ['.', './core', './rendering', './controls', './data', './demos', './style.css']

# Theme tests pass (src/theme/index.css now exists)
npx vitest run tests/theme/custom-properties.test.ts --reporter=verbose
```
</verification>

<success_criteria>
- package.json with name "sim-kit", all 7 export entries, sideEffects: ["*.css"]
- tsconfig.json with strict: true, moduleResolution: "bundler", noUncheckedIndexedAccess: true
- tsconfig.build.json extending tsconfig.json, src-only, emitDeclarationOnly
- src/types/index.ts with SimulationState, SimConfig, TickFn, PlaybackState, SpeedPreset (union type), ParameterSchema, ParameterValue, SimEvent
- src/theme/index.css with all 15 --sim-* tokens using exact values from CONTEXT.md plus @import "tailwindcss"
- All 6 layer index stubs exist (src/core, rendering, controls, data, demos, utils)
- npx tsc -p tsconfig.build.json --noEmit exits 0
- npx vitest run tests/theme/custom-properties.test.ts exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-core-engine/01-02-SUMMARY.md`
</output>
